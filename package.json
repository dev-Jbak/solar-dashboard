{
  "name": "solar",
  "private": true,
  "scripts": {
    "deploy:github": "npm run generate && node ./scripts/deploy-github",
    "deploy": "gh-pages -d .output/public -t true",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@nuxtjs/tailwindcss": "^6.8.0",
    "@types/node": "^18",
    "nuxt": "^3.5.2"
  },
  "dependencies": {
    "chart.js": "^4.3.0",
    "gh-pages": "^6.1.1",
    "vue-chartjs": "^5.2.0"
  }
}
